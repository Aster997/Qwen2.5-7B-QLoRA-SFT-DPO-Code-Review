# 项目决策点整理：Qwen2.5-7B 代码审查助手

> 每个决策按「结论 → Why → 备选方案 → 追问预判」组织，面试时能快速定位展开。

---

## 一、基座模型选择

### 决策：Qwen2.5-7B-Instruct

**Why：**
- 7B 是单卡 4090/L20 能跑 QLoRA 的最大级别，14B 在 DPO 阶段（4 次前向传播）必然 OOM
- 选 Instruct 而不是 Base：Instruct 已经有指令跟随能力，SFT 只需要注入代码审查领域知识，不用从零教会模型"理解指令→生成回答"这件事
- Qwen2.5 在代码理解能力上在开源 7B 级别处于第一梯队（CodeQwen 系列的能力已经 merge 进主线）

**备选：**
- CodeLlama-7B：代码能力强但中文弱，我的训练数据以中文为主
- DeepSeek-Coder-7B：代码能力好但彼时 DPO 支持还不完善
- Qwen2.5-14B：推理更好但 DPO 阶段单卡放不下

**追问预判：**
- Q: 为什么不用 Base 版本？
  A: Base 需要先做大量通用指令微调才能完成代码审查这种结构化输出任务。Instruct 已经完成了 RLHF 对齐，SFT 微调可以专注在域适配上，相当于站在巨人肩膀上。
- Q: 为什么不用 0.5B/1.5B 小模型先验证 pipeline？
  A: 应该的，回头看这是一个教训。小模型验证 pipeline 能更快发现数据质量问题。

---

## 二、训练策略：QLoRA (NF4 + 双重量化)

### 决策：4-bit NF4 量化 + LoRA，而非全参微调或 8-bit

**Why：**
- 全参微调 7B 需要约 56GB 显存（float16），4090 24GB 完全不可能
- 8-bit 量化可以做但 QLoRA 论文证明 NF4 在绝大多数任务上与 8-bit 无显著差异，且显存再省约 40%
- 双重量化（Double Quantization）：对量化常数再做一次量化，额外节省约 0.4GB/B 参数的显存，几乎无精度损失

**LoRA 目标层：`all`**
- LLaMA-Factory 的 `lora_target: all` 会注入到所有 Linear 层（Q/K/V/O/gate/up/down）
- 相比只注入 Q/V，全层注入在代码审查这种需要理解 + 生成长文本的任务上效果更好
- 显存开销增加约 30% 但在 48GB 卡上完全承受得住

**v1→v2→v3 LoRA 参数演进：**

| 版本 | rank | alpha | 数据量 | 为什么改 |
|------|------|-------|--------|----------|
| v1 (sft) | 16 | 32 | 800 条（重复率94%） | 初版，过拟合 |
| v2 (sft_pro) | 16 | 32 | 861 条 | 数据重建但量不够，rank 没动 |
| v3 | 32 | 64 | ~2500+ 条 | 数据量 3x，rank 翻倍匹配容量 |

**alpha/rank 比例始终 = 2 的理由：**
- LoRA 的有效学习率 = lr × (alpha/rank)
- 保持 alpha/rank = 2 意味着 rank 变化时不需要同步调学习率
- 如果 rank=32 但 alpha 还是 32（比例=1），等效学习率减半，模型会学不动

**追问预判：**
- Q: 为什么不用 GPTQ/AWQ？
  A: GPTQ/AWQ 是推理用的离线量化，不支持训练时反向传播。QLoRA 的 NF4 支持训练时梯度回传。
- Q: rank 怎么选的？有做消融实验吗？
  A: 没有系统消融（算力限制），但 rank 的选择有理论依据：QLoRA 论文建议 rank=16 对应约 1k 级数据，我们 2.5k+ 的数据量对应 rank=32 更合适，这是线性外推。

---

## 三、数据策略

### 3.1 v1 过拟合根因分析

**现象：** SFT v1 训练 loss 降到极低但 eval 指标几乎不提升
**根因：** 800 条数据中 94% 是重复的（仅 48 段唯一代码），模型只是背诵了 48 个 code→answer 的映射
**发现过程：** 写了 `analyze_sft_data.py` 对 input 做 MD5 去重统计，发现唯一代码只有 48 段
**教训：** 数据量 ≠ 唯一样本量，必须先做 EDA 再训练

### 3.2 v2 (sft_pro) 三来源数据设计

| 来源 | 条数 | 解决什么问题 |
|------|------|------------|
| DeepSeek 合成主集 | 675 | 主数据，按配比矩阵覆盖 6 语言 × 8 场景 × 3 难度 |
| 正确代码集 | 150 | 解决"模型对所有代码都挑刺"的偏见问题 |
| GitHub 真实代码 | 150 | 增加分布多样性，减少 DeepSeek 生成风格的偏倚 |

**为什么加正确代码集：**
训练集如果 100% 是有问题的代码，模型会学到"无论什么代码都要找问题"的 bias，对 `SELECT * FROM users WHERE id = ?`（参数化查询，没有注入风险）也会说"存在 SQL 注入"。加入正确代码样本让模型学会输出"代码写得好，无需修改"。

### 3.3 v3 数据全面升级

| 来源 | v2 条数 | v3 条数 | 变化说明 |
|------|---------|---------|----------|
| DeepSeek 合成主集 | 675 | ~2000 | 扩量 + 困难样本走 R1（推理能力更强）|
| 正确代码集 | 150 | ~420 | 占比稳定在 ~15%，防止"挑刺偏见" |
| GitHub 真实代码 | 150 | ~400 | 同上 |
| **对抗样本** | **0** | **~100** | **v3 新增**，训练边界拒绝能力 |
| **多轮追问** | **0** | **~80** | **v3 新增**，训练上下文连贯性 |
| **质量过滤** | **无** | **critic 过滤** | **v3 新增**，砍掉幻觉/答非所问的脏数据 |

**为什么加对抗样本（100条）：**
v2 评测中边界测试 B3（跑题）和 B4（角色扮演）全部失败。原因：训练集里完全没有这类拒绝样本，模型不知道应该拒绝。100 条对抗样本覆盖 20 种攻击类型（越狱/恶意代码/跑题/空输入等），教模型"这不是代码审查请求→礼貌拒绝并引导回来"。

**为什么加多轮追问（80条）：**
真实代码审查场景中用户会追问"还有别的问题吗""这个改法线程安全吗"。SFT 默认只学了单轮问答，多轮时会丢失上下文。80 条多轮样本通过 LLaMA-Factory 的 alpaca `history` 字段实现，不需要改数据格式。

**为什么加 critic 质量过滤：**
DeepSeek 生成的数据有 15-25% 存在幻觉（output 指出代码中不存在的函数/变量）、改进代码语法错误、答非所问等问题。不过滤直接进训练等于教模型说谎。用 V3 当 critic 打 4 个维度分（准确性/完整性/无幻觉/改进质量），overall ≤ 3 的样本淘汰。

**为什么主集用 V3、困难样本用 R1：**
- V3（deepseek-chat）：输出费 ¥8/M，生成速度快，适合简单/中等样本
- R1（deepseek-reasoner）：输出费 ¥16/M 且 reasoning token 也计费，但在"细微并发 bug""隐式资源泄漏"这类需要推理的困难场景中质量显著更好
- 对抗样本也用 R1：因为攻击场景需要 R1 的推理能力来设计自然的攻击 prompt，V3 生成的攻击太模式化

**追问预判：**
- Q: 为什么不用真实 Code Review 数据（如 GitHub PR comments）？
  A: 成本和版权。GitHub PR 评论需要大量清洗（噪声比极高，很多"LGTM"或无关讨论），且代码和评论的配对关系难以自动建立。合成数据+质量过滤在小规模项目中 ROI 更高。
- Q: 2500 条够吗？
  A: 对于 QLoRA 微调 7B 模型做特定任务，2000-5000 条经过清洗的高质量数据是合适范围。Alpaca 论文用 52k 条但那是通用能力，我们只需要注入代码审查这一个技能。
- Q: 配比矩阵是怎么定的？
  A: 参考 OWASP Top 10 和真实 Code Review 中最常见的问题分布。安全漏洞 20% 因为这是代码审查的核心价值。可读性只有 5% 因为这类问题模型已有的通用能力就能覆盖。

---

## 四、训练超参数

### 4.1 学习率 5e-5 → 3e-5

**Why：**
- QLoRA 论文推荐范围：1e-5 ~ 5e-5
- v2 用 5e-5 在 861 条数据上尚可，但 v3 数据量 3x + epoch 增加 → 总梯度更新步数约 4 倍
- 步数多 + 学习率高 = 容易在后期震荡。3e-5 是更保守的选择
- 等效调整：也可以保持 5e-5 但减少 epoch，但减 epoch 会让对抗/多轮这些少量样本学不够

### 4.2 Epoch 3 → 4

**Why：**
- 虽然数据量增加了，但新增的对抗样本（100 条）和多轮样本（80 条）占总量不到 7%
- 3 个 epoch 时这些少数类样本可能没被充分学到
- 4 个 epoch 给小类更多学习机会，过拟合靠 eval_loss 监控

### 4.3 Batch Size 1 → 2 + GA 16 → 8

**Why：**
- 等效 batch size 不变：1×16 = 2×8 = 16
- 物理 batch=2 意味着每个 step 的梯度估计更准（两条样本的梯度平均 vs 一条）
- gradient accumulation 从 16 降到 8 意味着参数更新频率翻倍，训练更稳定
- 这只在 L20 48GB 上可行，4090 24GB 放不下 batch=2

### 4.4 为什么没用 early stopping

**考虑过但没加的理由：**
- LLaMA-Factory 支持 early stopping 但需要额外配置 patience 等参数
- 4 个 epoch 的训练时间在 L20 上约 3 小时，即使多跑 1 epoch 也浪费不了多少钱
- 更可靠的做法：跑完 4 epoch，看 tensorboard 的 eval_loss 曲线，手动选最优 checkpoint
- 如果 eval_loss 在 epoch 2-3 就拐头，下次训练直接改 epoch 数

---

## 五、DPO 偏好对齐

### 5.1 v2 DPO 为什么退步（5.84 < SFT 6.26）

**根因诊断：**

| 失败模式 | 占退步样本的比例 | 原因 |
|----------|---------------|------|
| 对参数化 SQL 误报注入 | ~35% | DPO rejected 数据里有大量"SQL 存在注入"的样本，模型学到"遇到 SQL 就说注入" |
| 对正确代码过度批评 | ~40% | 偏好数据中"找问题"的样本占比过高，模型倾向于"什么代码都有问题" |
| 格式退化 | ~15% | DPO 的 cutoff_len=1024 截断了部分长回答的改进代码部分 |
| 偶发答非所问 | ~10% | 偏好数据分布与 SFT 真实输出分布不一致 |

**核心技术错误：off-policy DPO**
v2 的偏好数据是这样构造的：基于"SFT 可能有的 5 类弱点"，人工/规则编写 chosen 和 rejected。但这些 rejected 不是 SFT 模型真实会生成的输出 —— DPO 的数学前提是偏好对来自当前策略（π_ref）的采样。用人造的 rejected 做 DPO，等于在优化一个与实际策略无关的 loss surface。

### 5.2 v3 DPO 改进方向（on-policy）

**正确做法（等 SFT v3 训完后执行）：**
1. 部署 SFT v3 模型（vLLM 推理）
2. 对每条 input 采样 4 个回答（temperature=0.9）
3. 用 V3 当 judge 给 4 个回答打分
4. 最高分 → chosen，最低分 → rejected
5. 这就是 on-policy DPO：rejected 确实是 SFT 自己生成的弱回答，优化方向对了

**pref_beta 0.1 → 0.3：**
- beta 控制新策略偏离参考策略的幅度。beta 越大越保守
- v2 用 0.1 导致偏离过大（SFT 本来就没那么差），v3 用 0.3 更保守
- 理论依据：Rafailov et al. (2023) 建议在参考策略已经较好时提高 beta

**追问预判：**
- Q: 为什么不用 KTO 或 IPO 替代 DPO？
  A: KTO 不需要成对偏好数据（只要好/坏标签），理论上更适合我们的场景。但 LLaMA-Factory 对 KTO 的支持不如 DPO 成熟，且 DPO 的 on-policy 版本已经在工业界被充分验证。如果 v3 DPO 还不行，下一步会试 KTO。
- Q: 为什么不用 RLHF (PPO)？
  A: PPO 需要训练 reward model + policy + value model，三个模型同时在显存里，7B QLoRA 在 48GB 上也很吃力。DPO 只需要一个 policy + frozen reference，显存友好很多。

---

## 六、显卡选型

### 决策：AutoDL L20 48GB

| 卡 | 显存 | 单价 | 评价 |
|---|---|---|---|
| 4090D 24GB | 24GB | ~¥1.8/h | v2 用的，DPO 阶段 OOM 不得不砍 cutoff_len |
| **L20 48GB** | **48GB** | **~¥3/h** | **v3 选这个**，DPO 能用 cutoff_len=2048，batch=2 |
| A100 40GB | 40GB | ~¥6/h | 性能强但贵，40GB 对 DPO 也够但性价比不如 L20 |

**Why L20 而不是 A100：**
- QLoRA 训练是 compute-bound 而非 memory-bandwidth-bound
- L20 和 A100 在 QLoRA 场景下的训练速度差距约 15-20%（L20 的 Ada 架构功耗效率更高）
- 但价格差 2 倍，综合 ROI L20 赢

---

## 七、评测体系

### 7.1 自动评测（DeepSeek 当 judge）

**为什么用 LLM-as-judge 而非人工评测：**
- 50 条 × 3 模型 = 150 个回答需要评分，人工评测工作量太大且主观性高
- DeepSeek V3 作为 judge 的一致性比人类评分者更稳定（同一条回答多次评分的方差 < 0.3）
- 评分标准固化在 prompt 里：准确性(0-5) + 可行性(0-5) - 误报扣分

**局限性：**
- LLM judge 可能偏好与自己风格相似的回答（DeepSeek 评 DeepSeek 合成数据训出的模型有利益冲突）
- v3 可以考虑用 GPT-4o 或 Claude 做 judge 做 cross-validation

### 7.2 测试集设计

**v2 测试集（50条）的问题：**
- 没有对抗样本 → 无法评测边界拒绝能力
- 没有多轮样本 → 无法评测上下文连贯性

**v3 测试集（60条）的改进：**
- 按类型配额保证覆盖：对抗≥6、多轮≥4、代码正确≥5、每种语言≥3
- 测试集不参与训练和验证，是完全 hold-out 的

---

## 八、工程决策

### 8.1 为什么用 LLaMA-Factory 而不是手写训练循环

- LLaMA-Factory 封装了 QLoRA + DPO 的完整训练流程，一个 yaml 就能启动
- 内建 gradient checkpointing、flash attention、混合精度，不需要手动调
- 社区活跃（GitHub 50k+ stars），遇到问题能找到方案
- 劣势：灵活性受限，比如自定义 loss function 就很难改

### 8.2 断点续跑设计（generate_sft_data.py）

**Why：**
- 生成 2000 条数据需要 3-5 小时 API 调用，网络中断或 API 限速都可能导致中断
- 每 10 条自动保存 + 启动时加载已有数据，实现无感续跑
- 去重指纹（MD5 + SequenceMatcher）保证续跑时不会生成重复数据

### 8.3 critic 过滤批量化设计（filter_sft_data.py）

**Why 每次评 5 条而不是 1 条：**
- 5 条共享一个 system prompt，节省约 30% 的输入 token 费用
- DeepSeek 限速是 per-minute 的，1 次调用评 5 条 vs 5 次调用评 5 条，后者可能触发限速
- 为什么不评 10 条：单次 output 太长（10 条评分 JSON 约 2000 token），模型可能截断或丢失后面几条的评分

---

## 九、待做 / 已知未解决的问题

| 问题 | 状态 | 计划 |
|------|------|------|
| on-policy DPO 数据构造脚本 | 待写 | 等 SFT v3 训完后再做 |
| DPO v3 训练配置 yaml | 待写 | 基于 v2 改：beta 0.1→0.3, cutoff 1024→2048 |
| 跨模型 judge cross-validation | 未做 | 用 GPT-4o 对同一批数据评分，对比 DeepSeek judge 是否有偏 |
| token 长度分布检查 | 需要上机跑 | v3 数据可能因多轮样本导致更多超 2048 的情况，上机后先 check |
| README 更新 | 待做 | 等 v3 全流程跑完后统一更新 |
